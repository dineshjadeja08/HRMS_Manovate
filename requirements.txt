# Core Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.25
pymysql==1.1.0
cryptography==41.0.7
alembic==1.13.1

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0
PyJWT==2.8.0
bcrypt==4.0.1

# Pydantic for validation
Pydantic
pydantic-settings==2.1.0
email-validator==2.1.0

# Redis & Caching
# celery[redis]==5.3.4 requires redis >=4.5.2, <5.0.0 and !=4.5.5 — pin to a compatible 4.x release
redis==4.5.4
# hiredis is an optional C extension for faster Redis parsing. Building it on Windows
# requires Microsoft C++ Build Tools (MSVC). If you don't need the speedup, leave
# it out so pip uses the pure-Python parser. Uncomment the next line if you have
# the build tools installed and want the C-extension.
# hiredis==2.3.2

# Async Task Queue
celery==5.3.4
celery[redis]==5.3.4

# File handling & exports
openpyxl==3.1.2
pandas
reportlab==4.0.8

# HTTP Client
httpx==0.26.0

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
httpx==0.26.0

# Code Quality
black==24.1.1
flake8==7.0.0
mypy==1.8.0

# Monitoring & Logging
python-json-logger==2.0.7

# CORS
fastapi-cors==0.0.6
